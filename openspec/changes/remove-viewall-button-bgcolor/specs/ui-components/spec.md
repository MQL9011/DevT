## ADDED Requirements

### Requirement: View All Button Styling
CategorySection 组件中的 "View all" 按钮 SHALL 使用透明背景且无边框，呈现简洁的纯文本链接样式。

#### Scenario: Button renders with transparent background and no border
- **WHEN** CategorySection 组件渲染
- **THEN** View all 按钮应显示透明背景（无背景色）且无边框
